const locationSearch = document.getElementById('#location-search').textContent
const searchBtn = document.getElementById('#form-search-btn')
const pastSearches = document.getElementById('#past-searches')

// so when a user inputs data, i will need to run an api call based on the locaton they type in - think this will require both the weather api and coordinates api

function locationSearchFx () {
  // when i search a location i want the data correspodning to that location to populate in the main city container and also the five day container
  // i will need functions to do those as well
  // i think i will also need to fetch location data via an api pull
}